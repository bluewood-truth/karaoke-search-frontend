import styled from '@emotion/styled';
import {BoxProps} from '../types';

const Styled = (props: BoxProps) => {
  return {};
};

const options = {};

const Box = styled('div', options)(Styled);

export default Box;
